{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://novafabric.io/schemas/run-capsule.schema.json",
  "$comment": "schema_version 1.0.0; tracks design/spec/run-capsule-v1.md. Validates capsule.yaml. Sub-file schemas: tool-call -> https://novafabric.io/schemas/tool-call.schema.json, model-call -> https://novafabric.io/schemas/model-call.schema.json, env.lock -> https://novafabric.io/schemas/environment.schema.json, replay.yaml -> https://novafabric.io/schemas/replay-policy.schema.json, redaction-proof.json -> https://novafabric.io/schemas/secret-redaction.schema.json, lineage.jsonl -> https://novafabric.io/schemas/lineage-edge.schema.json.",
  "title": "RunCapsule",
  "description": "Top-level manifest (capsule.yaml) of a NovaFabric Run Capsule. One of two formats NovaFabric invents; the other is EvidenceBundle. Validates the YAML-1.2 manifest; sub-file schemas are referenced by URL.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "run_id",
    "created_at",
    "finished_at",
    "duration_ms",
    "status",
    "command",
    "capture_mode",
    "novafabric_version",
    "working_directory",
    "host",
    "environment_ref",
    "replay_policy_ref",
    "redaction_proof_ref",
    "trace_ref",
    "trace_root_span_id",
    "model_calls_ref",
    "tool_calls_ref",
    "assets_ref",
    "inputs",
    "outputs",
    "model_call_count",
    "tool_call_count",
    "mutating_tool_count"
  ],
  "properties": {
    "schema_version": {
      "type": "string",
      "pattern": "^1\\.",
      "description": "OAS v1.0 schema version. Must be 1.x.x (ADR-0034 §1)."
    },
    "run_id": {
      "$ref": "#/$defs/Ulid",
      "description": "Canonical 26-character ULID. Globally unique and lexicographically sortable."
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "UTC start time of the run (RFC 3339)."
    },
    "finished_at": {
      "type": "string",
      "format": "date-time",
      "description": "UTC end time of the run (RFC 3339). May equal created_at for instantaneous runs."
    },
    "duration_ms": {
      "type": "integer",
      "minimum": 0,
      "description": "Wall-clock duration in milliseconds."
    },
    "status": {
      "type": "string",
      "enum": ["success", "failure", "partial", "aborted"],
      "description": "Overall outcome of the run. 'partial' when streaming was interrupted; 'aborted' when killed by signal."
    },
    "command": {
      "type": "array",
      "items": {"type": "string"},
      "description": "The argv that produced the run, sanitized of secrets. Empty array for SDK-driven runs."
    },
    "capture_mode": {
      "type": "string",
      "enum": ["cli-wrapper", "sdk-decorator", "otel-import", "manual"],
      "description": "Mechanism used to capture this run."
    },
    "novafabric_version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+",
      "description": "Version of the NovaFabric tooling that wrote this capsule."
    },
    "working_directory": {
      "type": "string",
      "description": "Sanitized working directory path; home directory normalized to '~'."
    },
    "host": {
      "$ref": "#/$defs/Host",
      "description": "OS-level host identity at capture time. Sanitized per redaction policy."
    },
    "environment_ref": {
      "type": "string",
      "description": "Relative path to env.lock. Always 'env.lock' in v0.1.",
      "const": "env.lock"
    },
    "replay_policy_ref": {
      "type": "string",
      "description": "Relative path to replay.yaml. Always 'replay.yaml' in v0.1.",
      "const": "replay.yaml"
    },
    "redaction_proof_ref": {
      "type": "string",
      "description": "Relative path to redaction-proof.json.",
      "const": "redaction-proof.json"
    },
    "trace_ref": {
      "type": "string",
      "description": "Relative path to the OTel-compatible trace file.",
      "const": "trace.jsonl"
    },
    "trace_root_span_id": {
      "$ref": "#/$defs/SpanId",
      "description": "Root span ID of the trace tree in W3C trace-context format."
    },
    "model_calls_ref": {
      "type": "string",
      "description": "Relative path to model-calls.jsonl. Each line validates against https://novafabric.io/schemas/model-call.schema.json.",
      "const": "model-calls.jsonl"
    },
    "tool_calls_ref": {
      "type": "string",
      "description": "Relative path to tool-calls.jsonl. Each line validates against https://novafabric.io/schemas/tool-call.schema.json.",
      "const": "tool-calls.jsonl"
    },
    "assets_ref": {
      "type": "string",
      "description": "Relative path to assets.jsonl.",
      "const": "assets.jsonl"
    },
    "inputs": {
      "type": "array",
      "items": {"$ref": "#/$defs/Artifact"},
      "description": "Logical input artifacts. Empty array is valid."
    },
    "outputs": {
      "type": "array",
      "items": {"$ref": "#/$defs/Artifact"},
      "description": "Logical output artifacts. Empty array is valid."
    },
    "model_call_count": {
      "type": "integer",
      "minimum": 0,
      "description": "Count of records in model-calls.jsonl. Redundant but required for cheap validation."
    },
    "tool_call_count": {
      "type": "integer",
      "minimum": 0,
      "description": "Count of records in tool-calls.jsonl."
    },
    "mutating_tool_count": {
      "type": "integer",
      "minimum": 0,
      "description": "Count of tool calls with mutates: true."
    },
    "alias": {
      "type": "string",
      "pattern": "^[a-z0-9._-]{1,64}$",
      "description": "Human-friendly run name, e.g. 'flaky-agent-day-1'."
    },
    "parent_run_id": {
      "$ref": "#/$defs/Ulid",
      "description": "If this run was triggered by another captured run, its run_id."
    },
    "replay_of_run_id": {
      "$ref": "#/$defs/Ulid",
      "description": "If this run is a replay, the original run's run_id."
    },
    "replay_mode": {
      "type": "string",
      "enum": ["exact", "mocked", "semantic", "forensic"],
      "description": "Required when replay_of_run_id is set. The replay mode used."
    },
    "lineage_ref": {
      "type": "string",
      "description": "Relative path to lineage.jsonl. Optional in v0.2; required from v0.4. Each line validates against https://novafabric.io/schemas/lineage-edge.schema.json."
    },
    "attestations_ref": {
      "type": "string",
      "description": "Relative path to attestations/ directory. Populated by novafabric export-evidence."
    },
    "metadata": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "Free-form user labels. Values must be strings; must not contain secrets. Validators warn if any value matches a secret-detection pattern."
    },
    "git": {
      "$ref": "#/$defs/GitContext",
      "description": "Git repository context at capture time. Populated when run inside a git repo."
    },
    "exit_code": {
      "type": "integer",
      "description": "Process exit code. Present when capture_mode is 'cli-wrapper'."
    },
    "error": {
      "$ref": "#/$defs/ErrorInfo",
      "description": "Error detail when status is 'failure' or 'partial'. Sanitized for secrets."
    },
    "extensions": {
      "type": "object",
      "description": "Vendor extensions. Keys must be reverse-DNS-prefixed (e.g. 'io.langgraph.thread')."
    }
  },
  "if": {
    "required": ["replay_of_run_id"]
  },
  "then": {
    "required": ["replay_mode"]
  },
  "$defs": {
    "Ulid": {
      "type": "string",
      "pattern": "^[0-7][0-9A-HJKMNP-TV-Z]{25}$",
      "description": "26-character Crockford base32 ULID. Globally unique, lexicographically sortable, time-prefixed."
    },
    "SpanId": {
      "type": "string",
      "pattern": "^[0-9a-f]{16}$",
      "description": "16-character lowercase hex OTel span ID (W3C trace-context format)."
    },
    "ContentHash": {
      "type": "string",
      "pattern": "^sha256:[0-9a-f]{64}$",
      "description": "SHA-256 content hash in sha256:<hex> form."
    },
    "ExternalArtifactRef": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "uri", "hash"],
      "properties": {
        "kind": {
          "type": "string",
          "enum": ["url", "asset_ref", "local_path"],
          "description": "How to locate the external resource at replay time."
        },
        "uri": {
          "type": "string",
          "description": "URI of the external resource."
        },
        "hash": {
          "$ref": "#/$defs/ContentHash",
          "description": "SHA-256 of the resource bytes at capture time."
        },
        "fetched_at": {
          "type": "string",
          "format": "date-time",
          "description": "When the resource was fetched during capture (RFC 3339)."
        },
        "freshness_window": {
          "type": "string",
          "description": "ISO 8601 duration. Default from replay policy applies if absent."
        }
      }
    },
    "Artifact": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "path", "content_hash", "size_bytes", "media_type"],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Logical name, e.g. 'system-prompt' or 'final-response'."
        },
        "path": {
          "type": "string",
          "description": "Relative path under inputs/ or outputs/. Ignored when inline is true."
        },
        "content_hash": {
          "$ref": "#/$defs/ContentHash",
          "description": "SHA-256 of the artifact bytes."
        },
        "size_bytes": {
          "type": "integer",
          "minimum": 0
        },
        "media_type": {
          "type": "string",
          "description": "RFC 6838 media type, e.g. 'text/plain', 'application/json'."
        },
        "inline": {
          "type": "boolean",
          "description": "If true, the artifact bytes are inlined in inline_value; path is ignored.",
          "default": false
        },
        "inline_value": {
          "type": "string",
          "maxLength": 65536,
          "description": "UTF-8 content used when inline is true. Capped at 64 KiB; larger artifacts must use path."
        },
        "produced_by_call": {
          "oneOf": [
            {"$ref": "#/$defs/Ulid"},
            {"type": "null"}
          ],
          "description": "model_call_id or tool_call_id that produced this output artifact."
        },
        "external": {
          "$ref": "#/$defs/ExternalArtifactRef",
          "description": "Present when the artifact is stored by reference rather than embedded."
        }
      },
      "if": {
        "properties": {"inline": {"const": true}},
        "required": ["inline"]
      },
      "then": {
        "required": ["inline_value"]
      }
    },
    "GpuInfo": {
      "type": "object",
      "additionalProperties": false,
      "required": ["model", "vendor", "count"],
      "properties": {
        "model": {"type": "string"},
        "vendor": {"type": "string"},
        "driver": {"type": "string"},
        "vbios": {"type": "string"},
        "count": {"type": "integer", "minimum": 1}
      }
    },
    "Host": {
      "type": "object",
      "additionalProperties": false,
      "required": ["os", "arch", "cpu_count", "memory_bytes", "hostname_redacted"],
      "properties": {
        "os": {
          "type": "string",
          "enum": ["linux", "darwin", "windows"],
          "description": "Operating system."
        },
        "arch": {
          "type": "string",
          "description": "CPU architecture, e.g. 'x86_64', 'arm64'."
        },
        "python": {
          "type": "string",
          "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+",
          "description": "Python version major.minor.patch."
        },
        "cpu_count": {
          "type": "integer",
          "minimum": 1,
          "description": "Logical CPUs available to the process."
        },
        "memory_bytes": {
          "type": "integer",
          "minimum": 0,
          "description": "Total memory available to the process in bytes."
        },
        "gpu": {
          "type": "array",
          "items": {"$ref": "#/$defs/GpuInfo"},
          "description": "GPU inventory. Empty array if no GPU present."
        },
        "cuda": {
          "type": "string",
          "description": "CUDA version, e.g. '12.4'. Absent if no CUDA."
        },
        "slurm": {
          "type": "object",
          "additionalProperties": false,
          "required": ["cluster", "partition", "job_id"],
          "properties": {
            "cluster": {"type": "string"},
            "partition": {"type": "string"},
            "job_id": {"type": "string"}
          },
          "description": "SLURM context when running under a SLURM scheduler."
        },
        "kubernetes": {
          "type": "object",
          "additionalProperties": false,
          "required": ["namespace", "pod", "node"],
          "properties": {
            "namespace": {"type": "string"},
            "pod": {"type": "string"},
            "node": {"type": "string"}
          },
          "description": "Kubernetes context when running in a pod."
        },
        "hostname_redacted": {
          "type": "boolean",
          "description": "True when hostname was replaced with a stable hash per secret-policy-v0; false when the user opted in to recording it."
        }
      }
    },
    "GitContext": {
      "type": "object",
      "additionalProperties": false,
      "required": ["commit_sha", "branch", "dirty"],
      "properties": {
        "commit_sha": {
          "type": "string",
          "description": "Full or abbreviated HEAD SHA."
        },
        "branch": {
          "type": "string",
          "description": "Branch name or 'HEAD' if detached."
        },
        "dirty": {
          "type": "boolean",
          "description": "True if the working tree had uncommitted changes at capture."
        },
        "remote_url": {
          "type": "string",
          "description": "Remote URL, sanitized of credentials."
        }
      }
    },
    "ErrorInfo": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "message"],
      "properties": {
        "type": {
          "type": "string",
          "description": "Exception class name or error kind, e.g. 'ToolInvocationError', 'SignalAborted'."
        },
        "message": {
          "type": "string",
          "description": "Human-readable description, sanitized for secrets."
        },
        "traceback_ref": {
          "oneOf": [
            {"type": "string"},
            {"type": "null"}
          ],
          "description": "Relative path under outputs/ where the full traceback is stored."
        }
      }
    }
  }
}
