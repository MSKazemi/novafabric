{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://novafabric.io/schemas/prompt-composition-block.schema.json",
  "$comment": "Experimental — governed by ADR-0115 (accepted 2026-07-13). Graduated from the private design/spec/schemas/ on first implementation; schema_version 0.1.0. Tracks the private design/spec/prompt-composition-v0.md.",
  "title": "PromptCompositionBlock",
  "description": "Direct references a composed prompt asset declares, each snapshotted to a version+hash at register time.",
  "type": "array",
  "items": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "ref",
      "resolved_version",
      "resolved_hash"
    ],
    "properties": {
      "ref": {
        "type": "string",
        "minLength": 1
      },
      "resolved_version": {
        "type": "string",
        "minLength": 1
      },
      "resolved_hash": {
        "type": "string",
        "pattern": "^sha256:[0-9a-f]{64}$"
      },
      "selector_kind": {
        "enum": [
          "version",
          "label"
        ]
      }
    }
  }
}
