apiVersion: v1
kind: Service
metadata:
  name: {{ include "novafabric.fullname" . }}
  labels:
    {{- include "novafabric.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "novafabric.selectorLabels" . | nindent 4 }}
