# v0.19.1 — Dashboard autocomplete fixes

**Released:** 2026-05-18

Patch release — three dashboard input fields that accept database references now
populate with live suggestions from the server.

---

## What shipped

| Item | Panel | Fix |
|---|---|---|
| KG ingest `run_id` | `KGTab` → KGIngestPanel | `SuggestInput` wired to recent run IDs |
| Cost tab autocomplete | `CostTab` | model/provider picker populated from `/api/cost/pricing` |
| Holds tab autocomplete | `HoldsTab` | asset reference input now suggests registry names |

---

## Quality gates

- 91 serve tests pass
- ruff + mypy clean
- Static bundle: `DashboardApp.veaOATac.js`

---

## Upgrade

No Python or schema changes. Pull the new image or `pip install --upgrade novafabric`.
